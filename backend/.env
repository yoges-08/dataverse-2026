MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/dataverse_symposium
JWT_SECRET=dataverse_secret_key_2026
PORT=5000

# ---------------------------------------------------------------------------
# EMAIL SENDING
# ---------------------------------------------------------------------------
# To deliver emails to REAL student inboxes, un-comment and fill these using
# your Gmail account. (Gmail: turn on 2-Step Verification, then create an
# "App Password" and paste it as SMTP_PASS - do NOT use your normal password.)
#
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=aidssymposium2026@gmail.com
# SMTP_PASS=your-gmail-app-password
# SMTP_FROM="DATAVERSE 2026"
#
# Left blank/disabled (as below), emails are still SENT for testing through
# nodemailer's Ethereal test inbox and a clickable "OPEN EMAIL HERE" preview
# link is printed in the backend console.
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=